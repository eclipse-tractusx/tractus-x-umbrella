apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iatpmock.fullname" . }}-test-connection"
  labels:
    {{- include "iatpmock.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iatpmock.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
