apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edc-bundle.fullname" . }}-test-connection"
  labels:
    {{- include "edc-bundle.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edc-bundle.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never 