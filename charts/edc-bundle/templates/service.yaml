{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "edc-bundle.fullname" . }}
  labels:
    {{- include "edc-bundle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edc-bundle.selectorLabels" . | nindent 4 }}
{{- end }} 