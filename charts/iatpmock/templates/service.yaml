apiVersion: v1
kind: Service
metadata:
  name: {{ include "iatpmock.fullname" . }}
  labels:
    {{- include "iatpmock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iatpmock.selectorLabels" . | nindent 4 }}
